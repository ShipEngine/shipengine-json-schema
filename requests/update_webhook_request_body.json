{
  "$id": "update_webhook_request_body",
  "title": "update_webhook_request_body",
  "type": "object",
  "description": "An update webhook request body",
  "additionalProperties": false,
  "properties": {
    "url": {
      "allOf": [
        {
          "title": "url",
          "type": "string",
          "format": "url",
          "minLength": 1,
          "description": "A URL"
        }
      ],
      "description": "The url that the wehbook sends the request"
    }
  }
}